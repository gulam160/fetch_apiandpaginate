const LoadingIndicator = () => {
  const styles = {
    color: "red",
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
  };
  return <h1 style={styles}>Loading, Please wait...</h1>;
};
export default LoadingIndicator;
